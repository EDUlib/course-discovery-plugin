from django.conf.urls import url

from . import views

urlpatterns = [
    url('polymtl', views.polymtl, name='polymtl'),
    url('hec', views.hec, name='hec'),
    url('umontreal', views.umontreal, name='umontreal'),
    url('', views.index, name='index'),
]
